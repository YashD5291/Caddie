import SwiftUI

// MARK: - Theme

extension Color {
    /// Caddie's brand accent (#F97316). Single source of truth for the warm orange
    /// used across the loading overlay, meeting detail stats, and other accents.
    static let caddieAccent = Color(red: 0.976, green: 0.451, blue: 0.086)
}
