---
phase: 18-screen-capture-engine
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified: []
autonomous: false
requirements: [VID-05, VID-06, VID-07, STOR-04]
user_setup: []

must_haves:
  truths:
    - "A real display recording produces a playable HEVC .mov whose duration matches elapsed time and whose bitrate is at or below the preset cap (VID-06)"
    - "Caddie's own windows are visibly absent from the recorded video (VID-05)"
    - "A static-screen recording produces correct-duration output, not a seconds-long file (VID-06/VID-07)"
    - "The kill-9 gate passes on the local OS and the first-frame host-clock anchor lands within ~100 ms of capture start (STOR-04)"
    - "The 14.2-floor kill-9 re-run is recorded as an outstanding milestone-close TODO"
  artifacts: []
  key_links: []
---

<objective>
Final phase gate: the human verifies the hardware-dependent behaviors that unit tests and the scripted gate cannot fully cover — real capture playability, Caddie-window exclusion (visual), static-screen duration correctness, and the kill-9 recovery result. This is the go/no-go for Phase 18 before Phase 19 wiring.

Purpose: RESEARCH.md and 18-VALIDATION.md split validation into unit-testable pure logic (Plans 01–02) plus hardware/manual gates. ScreenCaptureKit needs Screen Recording permission and a real display; VID-05 exclusion and VID-06 bitrate/duration correctness are visual/measured checks. This checkpoint confirms all five phase success criteria are TRUE on real hardware.

Output: A recorded human verification of the phase success criteria, and a captured list of any deviations to feed into Phase 19 or the milestone-close checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-screen-capture-engine/18-VALIDATION.md
@.planning/phases/18-screen-capture-engine/18-02-SUMMARY.md
@.planning/phases/18-screen-capture-engine/18-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify real capture, VID-05 exclusion, static-screen duration, and kill-9 recovery</name>
  <files>none (human-verification checkpoint — no code changes)</files>
  <read_first>
    - .planning/phases/18-screen-capture-engine/18-VALIDATION.md (Manual-Only Verifications table — the exact behaviors, why-manual, and instructions this gate confirms)
    - .planning/phases/18-screen-capture-engine/18-02-SUMMARY.md (final start/stop signatures; how the host-clock anchor is surfaced/logged so the operator can read the ~100 ms offset)
    - .planning/phases/18-screen-capture-engine/18-03-SUMMARY.md (the kill-9 gate command, local run result, and the 14.2-floor re-run TODO to acknowledge)
  </read_first>
  <action>
    Human-verification checkpoint, no code changes. Guide the user through the five hardware checks listed in `<how-to-verify>`, collect PASS/FAIL plus the measured values (duration, bitrate, anchor offset) per item, and record them in `18-04-SUMMARY.md`. If any item fails, capture the exact observation so `/gsd:plan-phase 18 --gaps` can address it. The concrete steps and expected outcomes are in `<how-to-verify>`.
  </action>
  <what-built>
    A standalone `ScreenRecorder` (Sources/Recording/ScreenRecorder.swift) that records a display or window to a crash-safe, bitrate-capped HEVC `.mov` with a host-clock anchor, plus a DEBUG harness and `Scripts/kill9-recovery-gate.sh`. Unit tests (config/state/filter) and the scripted kill-9 gate are green. This checkpoint confirms the hardware behaviors that cannot run in CI.
  </what-built>
  <how-to-verify>
    Run each step and confirm the expected outcome. Report PASS/FAIL per item.

    1. Real capture + duration + bitrate (VID-06):
       - Build Debug: `make build`. Resolve the app binary (`Caddie.app/Contents/MacOS/Caddie`).
       - Record ~30 s of a normal (moving) display: `"$BIN" --screen-record-harness /tmp/caddie-cap.mov &`, wait ~30 s, then stop it (Ctrl-C / kill the pid gracefully if the harness supports it, else use the gate script's flow).
       - Open `/tmp/caddie-cap.mov` in QuickTime: it MUST play. Duration MUST be ~30 s (within a couple seconds).
       - Check bitrate: `mdls -name kMDItemTotalBitRate /tmp/caddie-cap.mov` (or `ffprobe` if available). For `.balanced` it should be at/below ~2.5 Mbps (allow codec overhead headroom). EXPECT: HEVC, not 40+ Mbps.

    2. Caddie-window exclusion (VID-05):
       - Record a display with a Caddie window (main window or Settings) visible on screen.
       - Scrub the output: Caddie's own windows MUST NOT appear anywhere in the frame. EXPECT: they are excluded even when on-screen.

    3. Static-screen duration (VID-06/VID-07):
       - Record ~2 minutes of a STATIC screen (do not move the mouse / change content).
       - Output duration MUST be ~2 minutes, NOT a few seconds. EXPECT: the last-frame keepalive kept the timeline advancing.

    4. kill-9 recovery + anchor (VID-07, STOR-04):
       - Run `bash Scripts/kill9-recovery-gate.sh`. EXPECT: `GATE PASS` (playable partial, duration >= elapsed - 10 s).
       - Confirm the harness/logs show `onFirstFrameHostTime` fired and the anchor (host tick -> seconds) is within ~100 ms of capture start (per 18-02-SUMMARY on how the anchor is surfaced/logged).

    5. Confirm the outstanding TODO: the kill-9 gate still needs a re-run on a macOS 14.2 machine before milestone release (local OS is macOS 26). Acknowledge this is tracked.
  </how-to-verify>
  <verify>Human confirms each of the five items PASS (or reports specific failures). No automated command — this gate is hardware/visual by nature (see 18-VALIDATION.md Manual-Only Verifications).</verify>
  <acceptance_criteria>
    - Item 1: `/tmp/caddie-cap.mov` opens and plays in QuickTime; measured duration within ~2 s of 30 s; measured bitrate <= ~2.5 Mbps (recorded in SUMMARY)
    - Item 2: Recorded frame with a Caddie window on-screen shows NO Caddie window content (VID-05 confirmed visually)
    - Item 3: ~2-minute static-screen recording has duration ~2 minutes (not seconds) — recorded in SUMMARY
    - Item 4: `bash Scripts/kill9-recovery-gate.sh` prints `GATE PASS`; anchor offset within ~100 ms recorded
    - Item 5: 14.2-floor re-run TODO explicitly recorded in 18-04-SUMMARY.md
    - Each item has an explicit PASS or FAIL with the measured value in 18-04-SUMMARY.md
  </acceptance_criteria>
  <done>All five items reported PASS by the human, OR failures captured for a --gaps plan; measured values and the 14.2-floor re-run TODO recorded in 18-04-SUMMARY.md.</done>
  <resume-signal>Type "approved" if all five items pass, or describe which item(s) failed and what you observed (so a --gaps plan can be generated).</resume-signal>
</task>

</tasks>

<verification>
- All five Phase 18 success criteria confirmed TRUE on real hardware, OR failures captured for gap closure
- kill-9 gate result recorded; 14.2-floor re-run tracked as a milestone-close TODO
</verification>

<success_criteria>
- Human-confirmed: real HEVC .mov plays with correct duration and capped bitrate (VID-06)
- Human-confirmed: Caddie windows absent from capture (VID-05)
- Human-confirmed: static-screen recording has correct duration (VID-06/VID-07)
- Human-confirmed: kill-9 gate passes and anchor lands within ~100 ms (VID-07, STOR-04)
</success_criteria>

<output>
After completion, create `.planning/phases/18-screen-capture-engine/18-04-SUMMARY.md`. Record per-item PASS/FAIL, measured values (duration, bitrate, anchor offset), and the tracked 14.2-floor re-run TODO. If any item failed, note it clearly so `/gsd:plan-phase 18 --gaps` can address it.
</output>
