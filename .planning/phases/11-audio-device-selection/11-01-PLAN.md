---
phase: 11-audio-device-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/Recording/AudioDeviceManager.swift
  - Tests/AudioDeviceManagerTests.swift
  - Sources/UI/Settings/SettingsView.swift
  - Sources/App/AppState.swift
autonomous: true
requirements:
  - AUD-01
  - AUD-02

must_haves:
  truths:
    - "User opens Settings and sees all available audio input devices in a picker"
    - "User selects a device and it becomes the active selection"
    - "User quits and relaunches -- previously selected device is still selected"
    - "If previously selected device is disconnected, user sees fallback to system default with a warning"
    - "Caddie's own aggregate devices (com.caddie.systemTap.*) do not appear in the picker"
  artifacts:
    - path: "Sources/Recording/AudioDeviceManager.swift"
      provides: "Device enumeration, selection persistence, fallback validation, change notification"
      exports: ["AudioDeviceManager", "AudioDeviceManager.InputDevice"]
    - path: "Tests/AudioDeviceManagerTests.swift"
      provides: "Unit tests for persistence, fallback, filtering logic"
      min_lines: 50
    - path: "Sources/UI/Settings/SettingsView.swift"
      provides: "Audio Input section with device picker and fallback warning"
      contains: "Audio Input"
    - path: "Sources/App/AppState.swift"
      provides: "AudioDeviceManager instance, initialized during app startup"
      contains: "audioDeviceManager"
  key_links:
    - from: "Sources/UI/Settings/SettingsView.swift"
      to: "Sources/Recording/AudioDeviceManager.swift"
      via: "Environment object binding to Picker selection"
      pattern: "audioDeviceManager\\.selectedDeviceUID"
    - from: "Sources/App/AppState.swift"
      to: "Sources/Recording/AudioDeviceManager.swift"
      via: "Property initialization and initialize() call"
      pattern: "audioDeviceManager\\.initialize"
    - from: "Sources/Recording/AudioDeviceManager.swift"
      to: "UserDefaults"
      via: "Persist/load selectedInputDeviceUID key"
      pattern: "selectedInputDeviceUID"
---

<objective>
Implement audio input device selection so users can browse available devices in Settings, pick one, and have that choice persist across app restarts.

Purpose: Foundation for v2.0 device-based audio capture. Phase 12 will wire this selection to actual recording; this phase is UI + persistence only.
Output: AudioDeviceManager class, AudioDeviceManagerTests, updated SettingsView with Audio Input picker, AppState wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-audio-device-selection/11-RESEARCH.md

@Sources/Detection/MicStateMonitor.swift
@Sources/App/AppState.swift
@Sources/UI/Settings/SettingsView.swift
@project.yml

<interfaces>
<!-- Existing interfaces the executor needs -->

From Sources/Detection/MicStateMonitor.swift (SimplyCoreAudio usage pattern):
```swift
import SimplyCoreAudio

// SimplyCoreAudio API surface used in this project:
// let coreAudio = SimplyCoreAudio()
// coreAudio.allInputDevices -> [AudioDevice]
// coreAudio.defaultInputDevice -> AudioDevice?
// AudioDevice.uid -> String?
// AudioDevice.name -> String
// AudioDevice.manufacturer -> String?
// AudioDevice.id -> AudioObjectID (UInt32, transient)
// AudioDevice.lookup(by: String) -> AudioDevice? (UID lookup)
// NotificationCenter: .deviceListChanged, .deviceIsRunningSomewhereDidChange
```

From Sources/App/AppState.swift (initialization pattern):
```swift
@MainActor
@Observable
final class AppState {
    private(set) var database: AppDatabase?
    private(set) var modelManager = ModelManager()
    private(set) var coordinator: RecordingCoordinator?
    var isInitialized = false
    var initError: String?
    func initialize() async { ... }
}
```

From Sources/UI/Settings/SettingsView.swift (section pattern):
```swift
struct SettingsView: View {
    var body: some View {
        Form {
            generalSection
            permissionsSection
            storageSection
            aboutSection
        }
        .formStyle(.grouped)
        .frame(width: 450)
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AudioDeviceManager with tests</name>
  <files>Sources/Recording/AudioDeviceManager.swift, Tests/AudioDeviceManagerTests.swift</files>
  <read_first>
    Sources/Detection/MicStateMonitor.swift
    Sources/App/AppState.swift
    .planning/phases/11-audio-device-selection/11-RESEARCH.md
    project.yml
  </read_first>
  <behavior>
    - testRefreshPopulatesInputDevices: After calling refresh(), availableInputDevices is non-empty (at least 1 device on any Mac with a built-in mic). Each device has a non-empty id (UID) and non-empty name.
    - testSelectionPersistsToUserDefaults: Setting selectedDeviceUID writes to UserDefaults under "selectedInputDeviceUID". Setting it to nil removes the key.
    - testInitializeLoadsPersistedSelection: When UserDefaults has a "selectedInputDeviceUID" value matching a real device, initialize() loads it into selectedDeviceUID.
    - testMissingDeviceFallsBackToDefault: When UserDefaults has a UID that doesn't match any current device, initialize() sets isUsingFallback=true and clears selectedDeviceUID (which also clears UserDefaults).
    - testFiltersCaddieAggregateDevices: Devices whose UID starts with "com.caddie.systemTap." are excluded from availableInputDevices. (Test the filtering logic by verifying no devices in the list have UIDs with that prefix.)
    - testResolvedDeviceIDReturnsNilWhenNoSelection: When selectedDeviceUID is nil, resolvedDeviceID() returns nil.
  </behavior>
  <action>
    TDD: Write tests first in Tests/AudioDeviceManagerTests.swift, then implement.

    Create Sources/Recording/AudioDeviceManager.swift as an @MainActor @Observable final class per AUD-01 and AUD-02:

    1. Define nested InputDevice struct (Identifiable, Hashable, Sendable) with:
       - id: String (UID, persistent)
       - name: String
       - manufacturer: String
       - audioDeviceID: AudioObjectID (transient)

    2. Published properties:
       - availableInputDevices: [InputDevice] (private(set))
       - selectedDeviceUID: String? (didSet triggers persistSelection())
       - isUsingFallback: Bool (private(set))

    3. Methods:
       - initialize(): calls refresh(), loadPersistedSelection(), validateSelection(), subscribeToChanges()
       - refresh(): enumerates coreAudio.allInputDevices, filters out UIDs starting with "com.caddie.systemTap.", maps to InputDevice structs
       - resolvedDeviceID() -> AudioObjectID?: looks up selectedDeviceUID via AudioDevice.lookup(by:)

    4. Private methods:
       - persistSelection(): writes/removes selectedDeviceUID to UserDefaults under key "selectedInputDeviceUID"
       - loadPersistedSelection(): reads from UserDefaults
       - validateSelection(): if stored UID not in device list, set isUsingFallback=true and clear selection
       - subscribeToChanges(): observe .deviceListChanged notification on .main queue, call refresh() + validateSelection()

    5. Use Logger(subsystem: "com.caddie.app", category: "AudioDeviceManager") for logging.
    6. Store the defaults key as: static let userDefaultsKey = "selectedInputDeviceUID"
    7. Clean up observer in deinit.

    IMPORTANT: Use the exact SimplyCoreAudio API patterns from MicStateMonitor.swift. The coreAudio property should be `private let coreAudio = SimplyCoreAudio()`.

    For tests: Use XCTest. Tests run against real CoreAudio hardware (matching existing test patterns -- no protocol mocking for SimplyCoreAudio). Clean up UserDefaults in setUp/tearDown using UserDefaults.standard.removeObject(forKey: "selectedInputDeviceUID"). Use @MainActor on the test class since AudioDeviceManager is MainActor-isolated.
  </action>
  <verify>
    <automated>xcodebuild test -project Caddie.xcodeproj -scheme Caddie -only-testing CaddieTests/AudioDeviceManagerTests -destination 'platform=macOS' 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class AudioDeviceManager" Sources/Recording/AudioDeviceManager.swift
    - grep -q "struct InputDevice" Sources/Recording/AudioDeviceManager.swift
    - grep -q "selectedInputDeviceUID" Sources/Recording/AudioDeviceManager.swift
    - grep -q "isUsingFallback" Sources/Recording/AudioDeviceManager.swift
    - grep -q "deviceListChanged" Sources/Recording/AudioDeviceManager.swift
    - grep -q "com.caddie.systemTap" Sources/Recording/AudioDeviceManager.swift
    - grep -q "func initialize()" Sources/Recording/AudioDeviceManager.swift
    - grep -q "func resolvedDeviceID" Sources/Recording/AudioDeviceManager.swift
    - grep -q "class AudioDeviceManagerTests" Tests/AudioDeviceManagerTests.swift
    - grep -q "testRefreshPopulatesInputDevices" Tests/AudioDeviceManagerTests.swift
    - grep -q "testSelectionPersistsToUserDefaults" Tests/AudioDeviceManagerTests.swift
    - grep -q "testMissingDeviceFallsBackToDefault" Tests/AudioDeviceManagerTests.swift
  </acceptance_criteria>
  <done>
    AudioDeviceManager enumerates devices, persists selection to UserDefaults, validates on startup with fallback, filters Caddie aggregate devices, and subscribes to hardware changes. All 6 unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: SettingsView picker and AppState wiring</name>
  <files>Sources/UI/Settings/SettingsView.swift, Sources/App/AppState.swift</files>
  <read_first>
    Sources/UI/Settings/SettingsView.swift
    Sources/App/AppState.swift
    Sources/Recording/AudioDeviceManager.swift
  </read_first>
  <action>
    Wire AudioDeviceManager into the app lifecycle and Settings UI per AUD-01 and AUD-02.

    **AppState.swift changes:**
    1. Add property: `private(set) var audioDeviceManager = AudioDeviceManager()`
    2. In initialize(), add `audioDeviceManager.initialize()` BEFORE the coordinator creation block (after AudioFileManager cleanup, before model loading). This is a synchronous MainActor call -- no await needed.

    **SettingsView.swift changes:**
    1. Add `@Environment(AppState.self)` property to access audioDeviceManager (or use the existing environment pattern -- check how other views access AppState).

    Actually, since SettingsView doesn't currently have AppState access, add:
    ```swift
    @State private var appState: AppState  // or use @Environment if AppState is in environment
    ```
    Check CaddieApp.swift to see how AppState is injected. Use the same pattern.

    2. Add a new computed property `audioInputSection` between generalSection and permissionsSection.

    3. The audioInputSection contains:
       - Section("Audio Input") with a Picker bound to audioDeviceManager.selectedDeviceUID
       - "System Default" option with tag String?.none
       - ForEach over availableInputDevices showing device.name with manufacturer in parentheses (when non-empty and not "Unknown")
       - Each device tagged with Optional(device.id) to match String? binding
       - Fallback warning: when isUsingFallback is true, show Label with exclamationmark.triangle icon, .font(.caption), .foregroundStyle(.orange)
       - Helper text: "Choose which microphone Caddie records from." in .caption .tertiary

    4. Add audioInputSection to the Form body between generalSection and permissionsSection.

    IMPORTANT: The Picker selection type MUST exactly match the tag type. Use `Binding<String?>` for selection and `String?.none` / `Optional(device.id)` for tags. Mismatched types cause the picker to silently fail (Pitfall 3 from research).

    To get a Binding from the @Observable AudioDeviceManager, use:
    ```swift
    @Bindable var audioDeviceManager = appState.audioDeviceManager
    ```
    or access via `Bindable(appState.audioDeviceManager)` in the body.
  </action>
  <verify>
    <automated>xcodebuild build -project Caddie.xcodeproj -scheme Caddie -destination 'platform=macOS' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "audioDeviceManager" Sources/App/AppState.swift
    - grep -q "audioDeviceManager.initialize()" Sources/App/AppState.swift
    - grep -q "Audio Input" Sources/UI/Settings/SettingsView.swift
    - grep -q "audioInputSection" Sources/UI/Settings/SettingsView.swift
    - grep -q "System Default" Sources/UI/Settings/SettingsView.swift
    - grep -q "selectedDeviceUID" Sources/UI/Settings/SettingsView.swift
    - grep -q "isUsingFallback" Sources/UI/Settings/SettingsView.swift
    - grep -q "exclamationmark.triangle" Sources/UI/Settings/SettingsView.swift
  </acceptance_criteria>
  <done>
    SettingsView shows Audio Input section with device picker between General and Permissions. AppState creates and initializes AudioDeviceManager on startup. App builds without errors. Full test suite passes.
  </done>
</task>

</tasks>

<verification>
1. All AudioDeviceManagerTests pass: `xcodebuild test -project Caddie.xcodeproj -scheme Caddie -only-testing CaddieTests/AudioDeviceManagerTests -destination 'platform=macOS'`
2. Full test suite passes (no regressions): `xcodebuild test -project Caddie.xcodeproj -scheme Caddie -destination 'platform=macOS'`
3. App builds successfully: `xcodebuild build -project Caddie.xcodeproj -scheme Caddie -destination 'platform=macOS'`
4. AudioDeviceManager.swift exists with all required exports
5. SettingsView contains Audio Input section with Picker
6. AppState initializes audioDeviceManager
</verification>

<success_criteria>
- AudioDeviceManager enumerates real audio input devices via SimplyCoreAudio
- Device selection persists to UserDefaults under "selectedInputDeviceUID"
- Missing device on startup triggers isUsingFallback=true and clears selection
- Caddie aggregate devices (com.caddie.systemTap.*) filtered from picker
- Device list updates dynamically via .deviceListChanged notification
- SettingsView shows "Audio Input" section with Picker + fallback warning
- AppState owns and initializes AudioDeviceManager
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/11-audio-device-selection/11-01-SUMMARY.md`
</output>
