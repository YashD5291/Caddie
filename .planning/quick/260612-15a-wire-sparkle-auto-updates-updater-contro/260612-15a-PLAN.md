---
phase: quick-260612-15a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Resources/Info.plist
  - Sources/App/CaddieApp.swift
  - Sources/UI/MenuBar/MenuBarView.swift
  - Sources/UI/Settings/SettingsView.swift
  - Tests/SparkleConfigTests.swift
  - scripts/release.sh
  - README.md
autonomous: true
requirements: [SPARKLE-WIRE, SPARKLE-RELEASE]

must_haves:
  truths:
    - "App launches with a live Sparkle updater (no inert/no-op controller)"
    - "User can click 'Check for Updates…' from the menu bar and trigger Sparkle's update check"
    - "User can toggle automatic update checks in Settings"
    - "Built Caddie.app Info.plist contains a non-empty SUFeedURL and SUPublicEDKey"
    - "scripts/release.sh produces a signed appcast.xml attached to the GitHub release"
  artifacts:
    - path: "Sources/App/CaddieApp.swift"
      provides: "App-scoped SPUStandardUpdaterController owned on MainActor"
      contains: "SPUStandardUpdaterController"
    - path: "Resources/Info.plist"
      provides: "SUFeedURL + SUPublicEDKey + SUEnableAutomaticChecks keys"
      contains: "SUFeedURL"
    - path: "Sources/UI/MenuBar/MenuBarView.swift"
      provides: "Check for Updates… menu action"
      contains: "Check for Updates"
    - path: "scripts/release.sh"
      provides: "generate_appcast step + gh release upload of appcast.xml"
      contains: "generate_appcast"
  key_links:
    - from: "Sources/UI/MenuBar/MenuBarView.swift"
      to: "SPUStandardUpdaterController"
      via: "checkForUpdates(_:) on the shared updater"
      pattern: "checkForUpdates"
    - from: "Resources/Info.plist"
      to: "GitHub releases/latest/download/appcast.xml"
      via: "SUFeedURL string value"
      pattern: "releases/latest/download/appcast.xml"
    - from: "scripts/release.sh"
      to: "GitHub release asset appcast.xml"
      via: "gh release upload --clobber"
      pattern: "gh release upload"
---

<objective>
Wire Sparkle 2.x auto-updates end-to-end in Caddie. Sparkle 2.9.0 is already linked but inert: no updater controller exists in Sources/, and Info.plist has no SUFeedURL/SUPublicEDKey. This plan makes updates functional: an app-scoped updater controller, user-facing "Check for Updates…" + auto-check toggle, the required Info.plist keys (real EdDSA public key from the login Keychain), and a release pipeline that generates + uploads a signed appcast.xml per GitHub release.

Purpose: Ship-once, update-forever — users get future releases automatically without re-downloading from a webpage.
Output: Live updater wiring, configured Info.plist, a 2-assertion config test, an extended release.sh, and a README "Auto-updates" note.

Note for SUMMARY: v1.2.0 already shipped WITHOUT this wiring. Auto-update benefits begin with the NEXT release (the first one to carry SUPublicEDKey AND an attached appcast.xml). This is expected — do not attempt to retrofit v1.2.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

# Sparkle integration facts (trust these — established by orchestrator):
# - EdDSA keys ALREADY exist in the login Keychain (generic password for "https://sparkle-project.org").
#   `generate_keys -p` prints the EXISTING public key. NEVER generate new keys; NEVER echo the private key.
# - Distribution = GitHub Releases. Feed URL is version-independent:
#   https://github.com/YashD5291/Caddie/releases/latest/download/appcast.xml
#   (GitHub's latest-asset redirect — every release MUST attach a fresh appcast.xml).
# - Sparkle tool paths live in DerivedData (multiple copies; glob at runtime, pick first existing):
#   ~/Library/Developer/Xcode/DerivedData/Caddie-*/SourcePackages/artifacts/sparkle/Sparkle/bin/{generate_keys,generate_appcast,sign_update}
# - generate_appcast signs each enclosure with the Keychain EdDSA key automatically.

<interfaces>
<!-- Sparkle 2.x public API the executor will use directly — no exploration needed. -->

import Sparkle

// Created once, owned at app scope. startingUpdater:true begins automatic-check scheduling.
final class SPUStandardUpdaterController: NSObject {
    init(startingUpdater: Bool,
         updaterDelegate: (any SPUUpdaterDelegate)?,
         userDriverDelegate: (any SPUStandardUserDriverDelegate)?)
    var updater: SPUUpdater { get }   // the live updater
}

// On SPUStandardUpdaterController (target/action friendly):
@IBAction func checkForUpdates(_ sender: Any!)   // presents the update UI

// On SPUUpdater:
var automaticallyChecksForUpdates: Bool { get set }   // bind the Settings toggle to this
var canCheckForUpdates: Bool { get }                  // KVO-observable; use for button enablement (optional, keep minimal)
</interfaces>

# How the project consumes Info.plist (verified):
# project.yml uses INFOPLIST_FILE: Resources/Info.plist (verbatim — NOT injected via `properties`).
# => Edit Resources/Info.plist directly. Do NOT add an `info:` block to project.yml.

# Test host: CaddieTests links against the Caddie app target, so in tests
# Bundle.main is Caddie.app and Bundle.main.object(forInfoDictionaryKey:) reads the built Info.plist.

# Existing UI section patterns to follow:
# - SettingsView.swift uses Form { Section("Title") { ... } } with .formStyle(.grouped) (see generalSection, GoogleAccountSection).
# - MenuBarView.swift actionsSection is a flat list of Button/Label and SettingsLink items.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire SPUStandardUpdaterController, UI controls, and Info.plist keys</name>
  <files>Sources/App/CaddieApp.swift, Sources/UI/MenuBar/MenuBarView.swift, Sources/UI/Settings/SettingsView.swift, Resources/Info.plist, Tests/SparkleConfigTests.swift</files>
  <behavior>
    SparkleConfigTests (reads Bundle.main built Info.plist):
    - test SUFeedURL is non-empty and equals "https://github.com/YashD5291/Caddie/releases/latest/download/appcast.xml"
    - test SUPublicEDKey is non-empty and base64-shaped (length >= 40, matches ^[A-Za-z0-9+/=]+$)
  </behavior>
  <action>
    Goal: a single, live, app-scoped Sparkle updater plus user-facing controls, plus the required Info.plist keys.

    1. Resources/Info.plist — add these keys (edit the file directly; project.yml uses it verbatim):
       - SUFeedURL (string): https://github.com/YashD5291/Caddie/releases/latest/download/appcast.xml
       - SUEnableAutomaticChecks (true)
       - SUPublicEDKey (string): the REAL public key. Obtain it at execution time by globbing for generate_keys and running it with -p:
           BIN=$(ls ~/Library/Developer/Xcode/DerivedData/Caddie-*/SourcePackages/artifacts/sparkle/Sparkle/bin/generate_keys 2>/dev/null | head -1)
           "$BIN" -p
         Paste the printed key verbatim into SUPublicEDKey. VERIFY it is non-empty and base64-ish before writing. NEVER print or store the private key; only the public key goes in the plist (it is public by design).

    2. Sources/App/CaddieApp.swift — own one updater controller at app scope, constructed on MainActor:
       - import Sparkle
       - The cleanest fit given AppDelegate is @MainActor: add a stored property on AppDelegate:
           let updaterController = SPUStandardUpdaterController(startingUpdater: true, updaterDelegate: nil, userDriverDelegate: nil)
         (Property initializer runs on MainActor since AppDelegate is @MainActor — satisfies Swift 6 isolation.)
       - Expose the updater so views reach it. Simplest: pass the controller into the SwiftUI environment. In CaddieApp.body, on the MenuBarExtra and Settings scenes, add `.environment(\.sparkleUpdaterController, appDelegate.updaterController)` OR pass via a lightweight @Observable holder. Pick the minimal approach: define a small EnvironmentKey for SPUStandardUpdaterController and inject appDelegate.updaterController into MenuBarView and SettingsView the same way appState is injected.
       - Do NOT create a second updater anywhere. One instance, app lifetime.

    3. Sources/UI/MenuBar/MenuBarView.swift — in actionsSection, add a "Check for Updates…" Button that calls updaterController.checkForUpdates(nil). Read the controller from the environment key defined in step 2. Place it near the Settings/Open Caddie items, before the Quit divider region as appropriate. Label with systemImage "arrow.down.circle".

    4. Sources/UI/Settings/SettingsView.swift — add an "Updates" Section (follow the Section("Title"){...} pattern) containing:
       - A Toggle("Automatically check for updates", isOn:) bound to updaterController.updater.automaticallyChecksForUpdates. Use a @State mirror initialized in .onAppear from the updater and write through on .onChange (the updater property is not Combine-published in a SwiftUI-friendly way; mirror it).
       - A "Check Now" Button calling updaterController.checkForUpdates(nil).
       Read updaterController from the same environment key. Keep it minimal — no CheckForUpdatesViewModel/KVO unless trivially clean.

    5. Tests/SparkleConfigTests.swift — XCTest reading Bundle.main.object(forInfoDictionaryKey:) for SUFeedURL and SUPublicEDKey, asserting per <behavior>.

    Follow CLAUDE.md: strict typing, no silent catches, small functions. TDD: write SparkleConfigTests first (it will fail until Info.plist keys exist), then add the keys + wiring to go green.
  </action>
  <verify>
    <automated>make test 2>&1 | tail -20  # SparkleConfigTests pass; full suite green; build succeeds with Sparkle imported</automated>
  </verify>
  <done>App owns one SPUStandardUpdaterController on MainActor; "Check for Updates…" appears in the menu bar; Settings has an Updates section with an auto-check toggle; built Info.plist has non-empty SUFeedURL + SUPublicEDKey + SUEnableAutomaticChecks; SparkleConfigTests pass; `make test` green.</done>
</task>

<task type="auto">
  <name>Task 2: Extend release.sh to generate + sign + upload appcast.xml, and document auto-updates</name>
  <files>scripts/release.sh, README.md</files>
  <action>
    Extend scripts/release.sh AFTER the stapling + checksum block (keep `set -euo pipefail` discipline; stage no secrets; never echo the private key).

    1. Locate the Sparkle bin directory via glob (pick first existing); error with actionable guidance if absent:
         SPARKLE_BIN=$(ls -d ~/Library/Developer/Xcode/DerivedData/Caddie-*/SourcePackages/artifacts/sparkle/Sparkle/bin 2>/dev/null | head -1)
         if [[ -z "$SPARKLE_BIN" || ! -x "$SPARKLE_BIN/generate_appcast" ]]; then
           echo "ERROR: Sparkle tools not found in DerivedData. Build the app once (make dmg) so SPM resolves Sparkle, then retry."
           exit 1
         fi

    2. Create a staging dir containing ONLY the newly stapled DMG (generate_appcast scans a directory and would include any DMGs present):
         APPCAST_STAGING=$(mktemp -d)
         trap 'rm -rf "$APPCAST_STAGING"' EXIT
         cp "$DMG_PATH" "$APPCAST_STAGING/"

    3. Run generate_appcast so enclosure URLs point at this release's GitHub asset path:
         "$SPARKLE_BIN/generate_appcast" "$APPCAST_STAGING" \
           --download-url-prefix "https://github.com/YashD5291/Caddie/releases/download/v${VERSION}/"
       (generate_appcast signs each enclosure with the Keychain EdDSA key automatically.)
       Move/copy the produced appcast.xml to "$PROJECT_DIR/appcast.xml".

    4. Verify the appcast is valid before proceeding:
         grep -q "sparkle:edSignature" "$PROJECT_DIR/appcast.xml" || { echo "ERROR: appcast.xml missing edSignature — Keychain signing failed"; exit 1; }
         grep -q "releases/download/v${VERSION}/${DMG_NAME}" "$PROJECT_DIR/appcast.xml" || { echo "ERROR: appcast.xml enclosure URL wrong"; exit 1; }

    5. Attach to the release. If `gh` is available AND the release exists, upload; otherwise print clear manual instructions:
         if command -v gh >/dev/null 2>&1 && gh release view "v${VERSION}" >/dev/null 2>&1; then
           gh release upload "v${VERSION}" "$PROJECT_DIR/appcast.xml" --clobber
           echo "Uploaded appcast.xml to release v${VERSION}."
         else
           echo "NOTE: Attach appcast.xml to the GitHub release v${VERSION} manually (gh not available or release not yet created)."
           echo "      Run after creating the release: gh release upload v${VERSION} \"$PROJECT_DIR/appcast.xml\" --clobber"
         fi

    6. Add a comment near the new block explaining: SUFeedURL relies on releases/latest/download/appcast.xml, so EVERY future release MUST attach appcast.xml or auto-updates silently stall.

    Do not reorder or weaken the existing notarize/staple/checksum logic — append only.

    README.md — add a short "Auto-updates" note (a new subsection or a feature-table row + paragraph): updates delivered via Sparkle from GitHub Releases; the signing key lives in the developer's login Keychain; each release attaches a signed appcast.xml; first release WITH wiring is the next one after v1.2.0. Per CLAUDE.md, README updates ship in the same pass.
  </action>
  <verify>
    <automated>bash -n scripts/release.sh && grep -q "generate_appcast" scripts/release.sh && grep -q "gh release upload" scripts/release.sh && grep -qi "auto-update" README.md && echo OK</automated>
  </verify>
  <done>release.sh appends an appcast generation+verification+upload block guarded by tool/release existence checks, never touching secrets and keeping set -euo pipefail; README documents the auto-update mechanism. `bash -n` passes and required tokens present.</done>
</task>

</tasks>

<verification>
- `make test` is green (SparkleConfigTests + full suite).
- App builds with `import Sparkle`; exactly one SPUStandardUpdaterController exists (grep Sources/ shows a single construction site).
- Built Caddie.app/Contents/Info.plist contains non-empty SUFeedURL and SUPublicEDKey (the SparkleConfigTests assert this against Bundle.main).
- `bash -n scripts/release.sh` passes; generate_appcast + gh upload paths present and guarded.
- No private key text anywhere in the repo or git diff (only SUPublicEDKey, which is public).
</verification>

<success_criteria>
- Sparkle is functional, not inert: a live updater starts at launch, user can check for updates from the menu bar, and toggle auto-checks in Settings.
- Info.plist carries the real EdDSA public key and the version-independent GitHub latest-asset feed URL.
- release.sh, run on the next release, produces and attaches a signed appcast.xml so future auto-updates work.
- All commits atomic (Task 1 = app wiring + config + test; Task 2 = release pipeline + docs), `make test` green before each, no Co-Authored-By, files staged explicitly, no secrets committed.
</success_criteria>

<output>
After completion, create `.planning/quick/260612-15a-wire-sparkle-auto-updates-updater-contro/260612-15a-SUMMARY.md`.
In the summary, explicitly note: v1.2.0 shipped without update wiring; auto-update benefits begin with the next release (first to carry SUPublicEDKey + an attached appcast.xml). This is expected, not a defect.
</output>
