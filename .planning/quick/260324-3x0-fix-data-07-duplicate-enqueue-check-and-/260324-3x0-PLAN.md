---
phase: quick
plan: 260324-3x0
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/Transcription/TranscriptionPipeline.swift
  - Tests/TranscriptionPipelineTests.swift
autonomous: true
requirements: [DATA-07, ERR-03]
must_haves:
  truths:
    - "Pipeline rejects enqueue for meetings with .transcribing status in DB"
    - "Pipeline continues to reject .done meetings"
    - "Pipeline continues to allow .error and .recording meetings"
    - "ERR-03 MicrophoneCapture weak self is already fixed (guard let self pattern in place)"
  artifacts:
    - path: "Sources/Transcription/TranscriptionPipeline.swift"
      provides: "Duplicate enqueue guard for .transcribing status"
      contains: "status == .transcribing"
    - path: "Tests/TranscriptionPipelineTests.swift"
      provides: "Test for .transcribing rejection"
      contains: "testRejectsDuplicateEnqueueForTranscribingMeeting"
  key_links:
    - from: "TranscriptionPipeline.enqueue()"
      to: "MeetingStatus.transcribing"
      via: "DB status check in enqueue guard"
      pattern: "status == .done || status == .transcribing"
---

<objective>
Close two audit gaps from v1.0-MILESTONE-AUDIT.md:

1. DATA-07: TranscriptionPipeline.enqueue() DB status check (line 91) only rejects `.done` meetings. Must also reject `.transcribing` to prevent duplicate enqueue of meetings being actively processed.

2. ERR-03: MicrophoneCapture.swift weak self -- ALREADY FIXED. Line 71 already uses `guard let self else { return }`. The audit was stale. This plan verifies and documents closure.

Purpose: Close remaining v1.0 audit gaps for milestone sign-off.
Output: Fixed enqueue guard + new test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@Sources/Transcription/TranscriptionPipeline.swift
@Tests/TranscriptionPipelineTests.swift

<interfaces>
From Sources/Transcription/TranscriptionPipeline.swift (line 91):
```swift
// Current: only rejects .done
if let status = currentStatus, status == .done {
```

From Sources/Storage/Meeting.swift:
```swift
enum MeetingStatus: String, Codable, DatabaseValueConvertible {
    case recording, transcribing, done, error
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add .transcribing rejection to enqueue() and test</name>
  <files>Sources/Transcription/TranscriptionPipeline.swift, Tests/TranscriptionPipelineTests.swift</files>
  <behavior>
    - testRejectsDuplicateEnqueueForTranscribingMeeting: Insert meeting with status=.transcribing, call enqueue(), expect onComplete with .failure(PipelineError.duplicateEnqueue)
    - testRejectsDuplicateEnqueueForDoneMeeting: (existing test) must still pass
    - testAllowsEnqueueForErrorMeeting: (existing test) must still pass -- .error meetings are retry-eligible
  </behavior>
  <action>
1. Write test FIRST in Tests/TranscriptionPipelineTests.swift:
   - Add `testRejectsDuplicateEnqueueForTranscribingMeeting` in the "Entry Guards (DATA-07, DATA-08)" MARK section, after `testRejectsDuplicateEnqueueForDoneMeeting`.
   - Pattern: same as `testRejectsDuplicateEnqueueForDoneMeeting` but insert meeting with `status: .transcribing` instead of `.done`.
   - Assert onComplete fires with `.failure` and error is `PipelineError`.

2. Run test -- confirm RED (test fails because .transcribing passes the guard).

3. Fix Sources/Transcription/TranscriptionPipeline.swift line 91:
   Change: `if let status = currentStatus, status == .done {`
   To: `if let status = currentStatus, status == .done || status == .transcribing {`
   Also update the comment on line 79 from "Check 2: Reject .done meetings" to "Check 2: Reject .done and .transcribing meetings".

4. Run test -- confirm GREEN.
  </action>
  <verify>
    <automated>cd /Users/yashdesai/Codebase/Fun/Caddie && xcodebuild test -project Caddie.xcodeproj -scheme Caddie -only-testing:CaddieTests/TranscriptionPipelineTests/testRejectsDuplicateEnqueueForTranscribingMeeting -only-testing:CaddieTests/TranscriptionPipelineTests/testRejectsDuplicateEnqueueForDoneMeeting -only-testing:CaddieTests/TranscriptionPipelineTests/testAllowsEnqueueForErrorMeeting -destination 'platform=macOS' 2>&1 | tail -20</automated>
  </verify>
  <done>
    - enqueue() rejects meetings with .transcribing status in DB via onComplete .failure callback
    - enqueue() still rejects .done meetings
    - enqueue() still allows .error meetings (retry path)
    - New test testRejectsDuplicateEnqueueForTranscribingMeeting passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify ERR-03 already resolved and update audit</name>
  <files>Sources/Recording/MicrophoneCapture.swift</files>
  <action>
1. Confirm MicrophoneCapture.swift line 71 already has `guard let self else { return }` (no code change needed).
2. Update .planning/v1.0-MILESTONE-AUDIT.md:
   - Change ERR-03 status from "partial" to "satisfied"
   - Update evidence to note the fix is already in place
   - Change DATA-07 status from "unsatisfied" to "satisfied"
   - Update the scores line from 26/28 to 28/28
   - Update status from gaps_found to passed (if DATA-01 partial is acceptable as info-only)
  </action>
  <verify>
    <automated>grep -n "guard let self else" /Users/yashdesai/Codebase/Fun/Caddie/Sources/Recording/MicrophoneCapture.swift | head -5</automated>
  </verify>
  <done>
    - MicrophoneCapture.swift confirmed using guard let self pattern
    - Audit report updated to reflect both gaps closed
  </done>
</task>

</tasks>

<verification>
- All existing TranscriptionPipelineTests pass (no regressions)
- New test testRejectsDuplicateEnqueueForTranscribingMeeting passes
- MicrophoneCapture.swift uses guard let self (already true)
- Audit report reflects closure
</verification>

<success_criteria>
- DATA-07 satisfied: enqueue() rejects both .done and .transcribing meetings
- ERR-03 satisfied: confirmed already fixed, audit updated
- All TranscriptionPipelineTests green
</success_criteria>

<output>
After completion, create `.planning/quick/260324-3x0-fix-data-07-duplicate-enqueue-check-and-/260324-3x0-SUMMARY.md`
</output>
